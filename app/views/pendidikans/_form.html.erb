<p id="notice"><%= notice %></p>
<table class="table table-striped">
<br>  
  <tbody>
      <tr>
        <%= form_for(@pendidikan) do |f| %>
          <% if @pendidikan.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@pendidikan.errors.count, "error") %> prohibited this pendidikan from being saved:</h2>

              <ul>
              <% @pendidikan.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>

          <tr>
            <td><div class="field"></td>
              <td><%= f.label :jenjang %></td>
              <td><%= f.text_field :jenjang %></td>
            </div>
          </tr>
          <br> 
          <tr>
          <td><div></td>
          <td></td>
            <div class="actions">
              <td><%= f.submit %></td>
            </div>
          </tr>
          </div>
          </div>
          </tbody>
          </table>
        <% end %>
