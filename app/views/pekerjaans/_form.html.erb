<p id="notice"><%= notice %></p>
<table class="table table-striped">
<br>  
  <tbody>
      <tr>
        <%= form_for(@pekerjaan) do |f| %>
          <% if @pekerjaan.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@pekerjaan.errors.count, "error") %> prohibited this pekerjaan from being saved:</h2>

              <ul>
              <% @pekerjaan.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
          <tr>
            <td><div class="field"></td>
            <td><%= f.label :jenis_pekerjaan %><br></td>
            <td><%= f.text_field :jenis_pekerjaan %></td>
            </div>
          </tr>
          <br> 
          <tr>
          <td><div></td>
          <td></td>
          <div class="actions">
            <td><%= f.submit %></td>
          </div>
          </tr>
          </div>
          </div>
          </tbody>
          </table>
        <% end %>
